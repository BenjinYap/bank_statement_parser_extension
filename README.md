# bank_statement_parser_extension

* why does balance protection not set to bills category?
* add a delete to main rows
* in dev mode, switching between modified row editors that have the same length of rows doesn't auto focus the select
* add original item to everything
* make total only update on amount "save"
* automatic td detect